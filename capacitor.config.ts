import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.nani.sueldos',
  appName: 'app-10',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
